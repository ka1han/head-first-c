/*
 编写一段程序，像下面那样读取两个整数，然后显示出前者是后者的百分之几。

 请输入两个整数。
 整数A：54
 整数B：84
 A的值是B的64%。
*/

#include <stdio.h>

int main(void)
{
  int A, B;
  scanf("%d", &A);
  scanf("%d", &B);
  
  printf("A的值是B的%d", A * 100 / B);

  return (0);
}
