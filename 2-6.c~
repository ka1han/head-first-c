/*
 整数和浮点数

 运行结果
 int 型变量 nx 的值：9
            nx / 2 : 9

 double 型变量 dx 的值：9.990000
               dx / 2 : 4.995000
*/

#include <stdio.h>
{
  int nx; //整数
  double dx; //浮点数

  nx=9.99;
  dx=9.99;

  printf("int 型变量nx的值:%d\n", nx);
  printf("          nx / 2:%d\n", nx /2);

  printf("double 型变量dx的值:%f\n", dx);
  printf("             dx / 2.0:%f\n", dx / 2.0);

  return (0);
}
